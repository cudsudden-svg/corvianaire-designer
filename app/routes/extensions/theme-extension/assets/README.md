This folder is build output from `apps/storefront-widget` (`npm run build`
in that package writes `customizer.js` + `customizer.css` directly here —
see its vite.config.ts). Don't hand-edit generated files; this README and
.gitkeep are the only files meant to be committed from this folder.
